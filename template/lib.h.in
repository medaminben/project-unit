#ifndef ${lib_NAME_UPPER}_H
#define ${lib_NAME_UPPER}_H
#include "${lib_ROOT_NAME}_export.h"

namespace ${lib_ROOT_NAME} {
    namespace ${lib_NAME} {
        unsigned int ${lib_ROOT_NAME_UPPER}_API get_the_answer(void) ;

        int ${lib_ROOT_NAME_UPPER}_API multiply(int, int) ;
    }
}

#endif // ${lib_UP_NAME}_H